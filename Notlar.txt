django textfield vs charfield

DateTimeField(auto_now_add=True)
	auto_now_add ?

winpty python manage.py süper kullanıcı oluşturur

python manage.py runserver
	sende böyle parametreli birşey yaz

robot testi ve mail ile doğrulama ekle

python manage.py flush
	//Verileri de sıfırlayarak resetler ve yeni bir superuser 
	oluşturmak gerekir.

sadece editlemeyi doğru kullanıcı ile sınırla

kullanıcı kendi postlarını ayrıca görse güzel olur

raise Http404 ve raise incele

tests.py kullan

bootstrap bak
https://getbootstrap.com/

Procfile

DEBUG ayarı hakkında

Deploy ederken veritabanı ve superuser oluşturmayı unutma.

Canlı Projeyi Güvenli Hâle Getirmek
# Heroku settings.
import os
import django_heroku
django_heroku.settings(locals())
if os.environ.get('DEBUG') == 'TRUE':
DEBUG = True
elif os.environ.get('DEBUG') == 'FALSE':
DEBUG = False

DEBUG = False olmasına rağmen kendi hata sayfalarımı göremedim.
	allowed_host = ['*']

get_object_or_404()
	
secret_key

20-5. Extended Learning Log: Add one feature to Learning Log, and push the change
to your live deployment. Try a simple change, such as writing more about the project on
the home page. Then try adding a more advanced feature, such as giving users the option
of making a topic public. This would require an attribute called public as part of the
Topic model (this should be set to False by default) and a form element on the
new_topic page that allows the user to change a topic from private to public. You’d then
need to migrate the project and revise views.py so any topic that’s public is visible to
unauthenticated users as well. Remember to migrate the live database after you’ve pushed
your changes to Heroku.




